import { SettingsScreen } from '@/components/dashboard/SettingsScreen';

export const metadata = { title: 'Settings — Ledgr' };

export default function SettingsPage() {
  return <SettingsScreen />;
}
