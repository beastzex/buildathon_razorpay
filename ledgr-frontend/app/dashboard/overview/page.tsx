import { OverviewScreen } from '@/components/dashboard/OverviewScreen';

export const metadata = { title: 'Overview — Ledgr' };

export default function OverviewPage() {
  return <OverviewScreen />;
}
